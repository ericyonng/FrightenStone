FS_ServerCore改成FS_NetEngine
添加
IConnector
把原先IConnector改成IAcceptor
Connector成功后会建立 connector需要注册一个回调，返回一个connector的sessionId

core新增GetUserBySession接口，user需要区分是服务类，还是用户类，使用Factory创建
User提供Close,Send等接口
FS_NetCore剔除所有配置耦合，改成传参
需要抽象一个ServerCore带Server的配置，ClientCore带Client配置
配置对象抽象ClientMgr与ServerMgr